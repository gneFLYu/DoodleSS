# Codex Interaction Log - 20260910T140758+0800

## Metadata

- Timestamp: 20260910T140758+0800
- Active branch: main
- Commit hash: ed4bc1a581c01bcf0084c14f3b838d6c9e5b5029
- Sync status: false

## Prompt

```text
Response annotations:
1. “当前 high-filtration fate audit 仍诚实返回 underdetermined（37 个 fact family、6 个周期机制、3 个 module kind）” — 在哪里查看
2. “对 j-adic vanishing obligation，单个有限向量即使被杀也不再足够，必须有 continuous、j-linear、complete-module certificate。” — 不建议复杂化, 我建议将这些 formal series 单独拎出来处理, 其相关的 differential 一般也是固定的

My request:
根据两个注释进行更改
```

## Assistant Final Response

```text
已按两个注释完成修改。

:codex-annotation{index="1"} 现在可直接打开 HFPSS-Studio 的 **Reviewing** 页面（`/review`）查看。页面顶部新增 **HIGH FILTRATION FATE AUDIT** 面板，显示当前 `underdetermined` 状态、37 个 fact family、6 个周期机制、3 个 module kind、5 个有限秩 obligation、1 个已覆盖项、4 个未决项，以及每个未决项的具体原因。原只读 API `/api/v2/review/periodic-fate-ledger` 仍保留。

:codex-annotation{index="2"} 已移除对 formal-series family 的 `continuous / j-linear / complete-module` 三项证书要求。现在它们在 [periodic fate ledger](<E:/课程/PACE2025_fly/HFPSS Q_8/DoodleSS/HFPSS-Studio/backend/data/review/formal_notes_periodic_fate_ledger.v1.json>) 中单独登记为 `fs.integer.bo` 与 `fs.sigma-i.bo`，保存固定 differential/permanent-cycle 模板；有限 killed-vector 不参与这类 family 的 rank audit。只有显式引用一个 admitted 且声明 `vanishing_outcome: killed` 的固定 family，才会解决相应 vanishing obligation，因此不会误称当前 mixed family 已全部死亡。

同步更新了 [RECORD.md](<E:/课程/PACE2025_fly/HFPSS Q_8/DoodleSS/HFPSS-Studio/RECORD.md>)、审计器、DKLLW E2 family 交叉引用、Reviewing UI，以及 `backend/static`/`public/static` 两份前端资源。

验证结果：`188 passed`；两份 JavaScript 语法检查通过；静态副本哈希一致；`git diff --check` 通过（仅有既存 `project.json` 换行提示）。
```

## Git Diff Summary

- Added a visible high-filtration fate-audit panel to Reviewing mode, including aggregate counts and exact unresolved-obligation reasons.
- Replaced j-adic continuous/complete-module certificate enforcement with a separate fixed-differential-family registry and routing rule.
- Linked DKLLW integer and sigma-i bo cycle families to the new formal-series family ids.
- Updated RECORD.md, backend audit output, synchronized frontend assets, and regression tests.

## Full Git Diff

```diff
diff --git a/.codex/CURRENT_STATE.md b/.codex/CURRENT_STATE.md
index 80a0181..389d1ff 100644
--- a/.codex/CURRENT_STATE.md
+++ b/.codex/CURRENT_STATE.md
@@ -1,10 +1,10 @@
 # Codex Project Memory: Current State
 
-Last updated: 2026-08-18 16:31:12 +0800
+Last updated: 2026-09-10 05:34:12 +0800
 
 ## Current Project Goal
 
-- Maintain HFPSS-Studio as an evidence-aware spectral-sequence research and review environment.
+- Verify Q8-HFPSS high-filtration vanishing from period-class-indexed facts while preserving finite, Witt, and j-adic module structure.
 
 ## Active Branch
 
@@ -12,28 +12,34 @@ Last updated: 2026-08-18 16:31:12 +0800
 
 ## Recent Decisions
 
-- Schema v7 represents F4 high-rank cells and matrix differentials explicitly while adapting legacy classes as distinct rank-one cells.
-- Canonical page transitions use only complete Danus-admitted maps; Researching previews may include candidates and partial maps.
-- General quotient-ring Gr枚bner bases and TateSS/SliceSS period pullbacks remain future extension layers.
+- Treat bo-pattern as a j=v1^4D^-1 formal-power-series module classification, never as a low-filtration or short-tower classification.
+- Use D^8 for invertible HFPSS object identity; use g^N D^(8Z) as an all-family HFPSS semiperiod, including bo; use the bidirectional g,D^8 lattice only with a positive-filtration Tate comparison certificate.
+- Audit finite 2-primary cells by exact killed-subspace rank, but require continuous j-linear complete-module certificates for j-adic towers.
 
 ## Known Issues
 
-- Interactive Codex Browser webview attachment timed out during final QA; automated HTTP/UI/template regressions pass.
+- The periodic fate audit remains underdetermined: one of five seed obligations is covered and four remain open.
+- formal_notes does not enumerate a complete E2 basis in all nine RO(Q8)/P sectors.
+- FN-2I-019 and FN-3I-010 are not yet fate-certifying, and the rank-two A+B target retains a one-dimensional quotient.
 
 ## Open Tasks
 
-- When Browser attachment is available, manually exercise rank-three cell editing, zeta matrix entry, combination ports, and quotient preview.
+- Enumerate each sector by module generators in one g-forward/D8 fundamental family, marking every cell finite-2-primary, witt-2-adic, or j-adic-formal-power-series.
+- Supply continuous complete-module differential certificates for j-adic bo towers before claiming their full high-filtration vanishing.
+- Review FN-2I-019 and FN-3I-010 for Danus admission and supply the missing rank in the A+B target cell.
 
 ## Latest Log References
 
+- .codex/logs/20260910T053339+0800.md
+- .codex/logs/20260908T205809+0800.md
+- .codex/logs/20260908T184330+0800.md
+- .codex/logs/20260908T184148+0800.md
+- .codex/logs/20260826T050831+0800.md
 - .codex/logs/20260818T163055+0800.md
 - .codex/logs/20260818T151555+0800.md
 - .codex/logs/20260817T121535+0800.md
 - .codex/logs/20260817T120539+0800.md
 - .codex/logs/20260812T183026+0800.md
-- .codex/logs/20260811T122823+0800.md
-- .codex/logs/20260811T111736+0800.md
-- .codex/logs/20260811T111300+0800.md
 
 ## Collaboration Notes
 
diff --git a/backend/app.py b/backend/app.py
index 034b267..77a2b53 100644
--- a/backend/app.py
+++ b/backend/app.py
@@ -66,6 +66,10 @@ from domain.page_periodicity import (
 from domain.models import ClassNode, Differential, Grade, Project, Proposition, Workspace, new_id, project_from_dict, project_to_dict
 from domain.project_io import ProjectImportValidationError, import_digest, prepare_project_import
 from domain.periods import LEGACY_DIFFERENTIAL_PERIODS
+from domain.periodic_fate_ledger import (
+    audit_periodic_fate_ledger,
+    load_periodic_fate_ledger,
+)
 from domain.periodicity import (
     PeriodicityOperationError,
     materialize_periodic_translate,
@@ -209,6 +213,16 @@ def legacy_catalog_entry(entry_id: str):
         return jsonify({"error": str(error)}), 422
 
 
+@app.get("/api/v2/review/periodic-fate-ledger")
+def periodic_fate_ledger():
+    """Expose the review-only period-class ledger with a computed audit."""
+    try:
+        ledger = load_periodic_fate_ledger()
+    except (FileNotFoundError, json.JSONDecodeError) as error:
+        return jsonify({"error": str(error)}), 422
+    return jsonify({"ledger": ledger, "audit": audit_periodic_fate_ledger(ledger)})
+
+
 @app.get("/api/project/export")
 def export_project():
     """Download the complete Studio project JSON, not a chart-only snapshot."""
diff --git a/backend/data/project.json b/backend/data/project.json
index a8089a8..cccabd5 100644
--- a/backend/data/project.json
+++ b/backend/data/project.json
@@ -8426,7 +8426,7 @@
       "certificate_proposition_id": "",
       "supporting_proposition_ids": [],
       "status": "established",
-      "source_ref": "DKLLW24, Table 7 and 搂6.1.2 (PDF p. 40); excludes low-filtration v1-local classes"
+      "source_ref": "DKLLW24 Lemma 4.7, d3 families, and 搂6.1.2: forward g=kD^3 semiperiod for all chart families, including j-adic bo towers; g^-1 still requires Tate comparison"
     },
     {
       "id": "period_sigma_D_E2",
@@ -8498,7 +8498,7 @@
       "certificate_proposition_id": "",
       "supporting_proposition_ids": [],
       "status": "established",
-      "source_ref": "DKLLW24, 搂6.1.2 (PDF p. 40); excludes low-filtration v1-local classes"
+      "source_ref": "DKLLW24 d3 families and 搂6.1.2: forward g semiperiod includes j-adic bo towers; g^-1 requires Tate comparison"
     },
     {
       "id": "period_diff_two_d3_u",
@@ -9491,7 +9491,7 @@
       "scope": "Integer-graded Q8 HFPSS only. Applies on E_r for r>=3; the cited chart statement does not authorize E2 copies.",
       "exclusions": [
         "No E2 propagation: the source specifies D-periodicity on E2 and D^8-periodicity on other pages.",
-        "No automatic g=kD^3 propagation: section 6.1.2 excludes v1-local classes in low filtration.",
+        "No automatic g=kD^3 materialization in this D^8-only rule: g is stored as a forward semiperiod for all families, including j-adic bo towers, and g^-1 requires a Tate comparison certificate.",
         "No propagation to other workspaces without a separately scoped rule."
       ],
       "source_ref": "DKLLW24, Proposition 4.1 (local PDF p. 25; journal p. 19): D^8 is invertible in bidegree (64,0); section 6.1.2 (local PDF p. 51; journal p. 40): non-E2 HFPSS pages are (64,0)-periodic by D^8."
@@ -9744,4 +9744,4 @@
       "virtual": true
     }
   ]
-}
\ No newline at end of file
+}
diff --git a/backend/domain/dkllw_fact_chain.py b/backend/domain/dkllw_fact_chain.py
index a1f28c9..0fd8af9 100644
--- a/backend/domain/dkllw_fact_chain.py
+++ b/backend/domain/dkllw_fact_chain.py
@@ -208,7 +208,7 @@ def ensure_dkllw_fact_chain(project: Project) -> Project:
         _conclusion("permanent-cycle", "Q8-HFPSS", cycles=["h1", "h2", "g=kD^3"], period_family_id="period_integer_D8"),
         ["prop_chain_c2_hurewicz_pc", "prop_chain_2bss_int_survivors"], "Restriction to C2",
         "DKLLW24 main.tex lines 1327-1342, Lemma 4.7",
-        "These permanent cycles transport differentials by Leibniz; g also gives the chart's (20,4) repetition away from low v1-local classes.",
+        "These permanent cycles transport differentials by Leibniz. The forward g action gives the chart's (20,4) semiperiod on all families, including j-adic bo towers; only g^{-1} needs a Tate comparison certificate.",
     )
     fact(
         "prop_chain_q8_usigma_pc", "permanent-cycle",
diff --git a/backend/domain/migrations.py b/backend/domain/migrations.py
index e227bac..4e0363f 100644
--- a/backend/domain/migrations.py
+++ b/backend/domain/migrations.py
@@ -444,7 +444,7 @@ def migrate_dkl24_q8_corrections(project: Project) -> Project:
                 id="period_integer_g", name="g=kD^3 (20,4)-period", workspace_id="ws_integer", rank=1,
                 generators=[PeriodGenerator(Grade(stem=20, filtration=4), "g=kD^3")], valid_from_page=2,
                 valid_to_page="infinity", status="established",
-                source_ref="DKLLW24, Table 7 and 搂6.1.2 (PDF pp. 18, 40); excludes low-filtration v1-local classes",
+                source_ref="DKLLW24 Lemma 4.7, d3 families, and 搂6.1.2: forward g=kD^3 semiperiod for all chart families, including j-adic bo towers; g^-1 still requires Tate comparison",
             ),
         ])
     for family in project.period_families:
@@ -455,13 +455,13 @@ def migrate_dkl24_q8_corrections(project: Project) -> Project:
             family.valid_from_page = 3
             family.source_ref = "DKLLW24, Proposition 4.1 (local PDF p. 25; journal p. 19) and section 6.1.2 (local PDF p. 51; journal p. 40)"
         if family.id == "period_integer_g":
-            family.source_ref = "DKLLW24, Table 7 and 搂6.1.2 (PDF p. 40); excludes low-filtration v1-local classes"
+            family.source_ref = "DKLLW24 Lemma 4.7, d3 families, and 搂6.1.2: forward g=kD^3 semiperiod for all chart families, including j-adic bo towers; g^-1 still requires Tate comparison"
 
     if sigma and "period_sigma_D8" not in known:
         project.period_families.extend([
             PeriodFamily("period_sigma_D_E2", "(*-sigma_i) E2 D-period", "ws_sigma_i", 1, [PeriodGenerator(Grade(stem=8), "D")], 2, 2, status="established", source_ref="DKLLW24, 搂6.1.2 (PDF p. 40)"),
             PeriodFamily("period_sigma_D8", "(*-sigma_i) D^8 64-period", "ws_sigma_i", 1, [PeriodGenerator(Grade(stem=64), "D^8")], 3, "infinity", status="established", source_ref="DKLLW24, section 6.1.2 (local PDF p. 51; journal p. 40)"),
-            PeriodFamily("period_sigma_g", "(*-sigma_i) g=kD^3 (20,4)-period", "ws_sigma_i", 1, [PeriodGenerator(Grade(stem=20, filtration=4), "g=kD^3")], 2, "infinity", status="established", source_ref="DKLLW24, 搂6.1.2 (PDF p. 40); excludes low-filtration v1-local classes"),
+            PeriodFamily("period_sigma_g", "(*-sigma_i) g=kD^3 (20,4)-period", "ws_sigma_i", 1, [PeriodGenerator(Grade(stem=20, filtration=4), "g=kD^3")], 2, "infinity", status="established", source_ref="DKLLW24 d3 families and 搂6.1.2: forward g semiperiod includes j-adic bo towers; g^-1 requires Tate comparison"),
         ])
     return project
 
diff --git a/backend/domain/periodicity.py b/backend/domain/periodicity.py
index 0dd4bd5..0d54e86 100644
--- a/backend/domain/periodicity.py
+++ b/backend/domain/periodicity.py
@@ -29,8 +29,9 @@ class PeriodicityOperationError(ValueError):
 def ensure_source_backed_q8_periodicity_rules(project: Project) -> Project:
     """Install exactly one D^8 rule for the established integer Q8 branch.
 
-    The source also discusses a (20,4) g=kD^3 period with low-filtration
-    exceptions.  That exception is not encoded as an automatic operation.
+    The project separately records the (20,4) forward g=kD^3 semiperiod for
+    every family, including j-adic bo towers.  It is not encoded by this
+    *D^8-only* materializer because g is not being inverted here.
     """
 
     integer = next((item for item in project.workspaces if item.id == "ws_integer"), None)
@@ -57,7 +58,7 @@ def ensure_source_backed_q8_periodicity_rules(project: Project) -> Project:
         ),
         exclusions=[
             "No E2 propagation: the source specifies D-periodicity on E2 and D^8-periodicity on other pages.",
-            "No automatic g=kD^3 propagation: section 6.1.2 excludes v1-local classes in low filtration.",
+            "No automatic g=kD^3 materialization in this D^8-only rule: g is stored as a forward semiperiod for all families, including j-adic bo towers, and g^-1 requires a Tate comparison certificate.",
             "No propagation to other workspaces without a separately scoped rule.",
         ],
         source_ref=D8_RULE_SOURCE,
diff --git a/backend/domain/seed.py b/backend/domain/seed.py
index 28c795d..84eacf1 100644
--- a/backend/domain/seed.py
+++ b/backend/domain/seed.py
@@ -417,16 +417,16 @@ def research_project() -> Project:
             id="period_integer_g", name="g=kD^3 (20,4)-period", workspace_id=integer.id, rank=1,
             generators=[PeriodGenerator(Grade(stem=20, filtration=4), "g=kD^3")],
             valid_from_page=2, valid_to_page="infinity", status="established",
-            source_ref="DKLLW24, Table 7 and \u00a76.1.2 (PDF pp. 18, 40); excludes low-filtration v1-local classes",
+            source_ref="DKLLW24 Lemma 4.7, d3 families, and 搂6.1.2: forward g=kD^3 semiperiod for all chart families, including j-adic bo towers; g^-1 still requires Tate comparison",
         ),
     ]
     for family in project.period_families:
         if family.id == "period_integer_g":
-            family.source_ref = "DKLLW24, Table 7 and 搂6.1.2 (PDF p. 40); excludes low-filtration v1-local classes"
+            family.source_ref = "DKLLW24 Lemma 4.7, d3 families, and 搂6.1.2: forward g=kD^3 semiperiod for all chart families, including j-adic bo towers; g^-1 still requires Tate comparison"
     project.period_families.extend([
         PeriodFamily("period_sigma_D_E2", "(*-sigma_i) E2 D-period", sigma.id, 1, [PeriodGenerator(Grade(stem=8), "D")], 2, 2, status="established", source_ref="DKLLW24, 搂6.1.2 (PDF p. 40)"),
         PeriodFamily("period_sigma_D8", "(*-sigma_i) D^8 64-period", sigma.id, 1, [PeriodGenerator(Grade(stem=64), "D^8")], 2, "infinity", status="established", source_ref="DKLLW24, 搂6.1.2 (PDF p. 40)"),
-        PeriodFamily("period_sigma_g", "(*-sigma_i) g=kD^3 (20,4)-period", sigma.id, 1, [PeriodGenerator(Grade(stem=20, filtration=4), "g=kD^3")], 2, "infinity", status="established", source_ref="DKLLW24, 搂6.1.2 (PDF p. 40); excludes low-filtration v1-local classes"),
+        PeriodFamily("period_sigma_g", "(*-sigma_i) g=kD^3 (20,4)-period", sigma.id, 1, [PeriodGenerator(Grade(stem=20, filtration=4), "g=kD^3")], 2, "infinity", status="established", source_ref="DKLLW24 d3 families and 搂6.1.2: forward g semiperiod includes j-adic bo towers; g^-1 requires Tate comparison"),
     ])
     # The shipped research workspace deliberately includes the small,
     # D-localized, source-backed E2 catalogue.  Arbitrary persisted projects
diff --git a/backend/static/app.js b/backend/static/app.js
index 4f0a5f8..bb63790 100644
--- a/backend/static/app.js
+++ b/backend/static/app.js
@@ -4,6 +4,7 @@ const state = {
   catalogEntries: [],
   catalogMode: false,
   logicGraph: { nodes: [], edges: [] },
+  periodicFateLedger: null,
   history: { undo_depth: 0, redo_depth: 0, undo_label: null, redo_label: null },
   workspaceId: null,
   pageByWorkspace: new Map(),
@@ -1192,10 +1193,53 @@ function renderLogicGraph() {
     : `${nodes.length} typed nodes 路 ${edges.length} visible evidence edges.`;
 }
 
+function renderPeriodicFateAudit() {
+  const payload = state.periodicFateLedger || {};
+  const audit = payload.audit || {};
+  const ledger = payload.ledger || {};
+  const status = audit.status || "unavailable";
+  const statusNode = $("#periodic-fate-audit-status");
+  statusNode.textContent = status;
+  statusNode.dataset.status = status;
+
+  const metrics = [
+    ["Fact families", audit.fact_family_count],
+    ["Period mechanisms", audit.period_mechanism_count],
+    ["Module kinds", audit.module_kind_count],
+    ["Finite obligations", audit.finite_rank_obligation_count],
+    ["Covered", audit.covered_obligation_count],
+    ["Formal-series families", audit.formal_series_family_count],
+  ];
+  $("#periodic-fate-audit-metrics").innerHTML = metrics.map(([label, value]) => (
+    `<div><strong>${Number(value || 0)}</strong><span>${escapeHtml(label)}</span></div>`
+  )).join("");
+
+  const formalPolicy = ledger.formal_series_policy || {};
+  $("#formal-series-audit-note").textContent = formalPolicy.storage_rule
+    ? `Formal series are separate from finite-rank audit. ${formalPolicy.storage_rule}`
+    : "Formal-series families are handled separately from finite-rank obligations.";
+
+  const unresolved = (audit.obligations || []).filter((item) => !item.covered);
+  $("#periodic-fate-obligations").innerHTML = unresolved.length
+    ? `<h3>${unresolved.length} unresolved obligations</h3>${unresolved.map((item) => {
+      const reasons = (item.unresolved_reasons || []).length
+        ? item.unresolved_reasons.map((reason) => `<li>${escapeHtml(reason)}</li>`).join("")
+        : "<li>No resolution has been recorded.</li>";
+      const representative = item.period_class_id || item.id;
+      return `<article class="audit-obligation"><strong>${escapeHtml(item.id)}</strong><span>${escapeHtml(representative)}</span><ul>${reasons}</ul></article>`;
+    }).join("")}`
+    : '<p class="empty">All recorded high-filtration obligations are covered.</p>';
+}
+
 async function loadReviewPage() {
-  const [project, logicGraph] = await Promise.all([api("/api/project"), api("/api/v2/logic-graph")]);
+  const [project, logicGraph, periodicFateLedger] = await Promise.all([
+    api("/api/project"),
+    api("/api/v2/logic-graph"),
+    api("/api/v2/review/periodic-fate-ledger"),
+  ]);
   state.project = project;
   state.logicGraph = logicGraph;
+  state.periodicFateLedger = periodicFateLedger;
   if (!project.workspaces.some((item) => item.id === state.workspaceId)) state.workspaceId = defaultWorkspaceId();
   const selector = $("#review-workspace-select");
   selector.innerHTML = project.workspaces.map((item) => (
@@ -1208,6 +1252,7 @@ async function loadReviewPage() {
     + `<span>${Number(admission.review_queue || 0)} in review</span>`
     + `<small>${escapeHtml(admission.policy || "Premise-complete facts enter the admitted DAG.")}</small>`
   );
+  renderPeriodicFateAudit();
   renderProofTree();
   syncLayoutHeight();
 }
diff --git a/backend/static/style.css b/backend/static/style.css
index 4ae8314..abcb1b5 100644
--- a/backend/static/style.css
+++ b/backend/static/style.css
@@ -1027,6 +1027,7 @@ body.catalog-mode #find-differential-candidates {
 }
 
 .review-intro,
+.review-audit-card,
 .review-controls,
 .review-graph-card,
 .review-ledger-card {
@@ -1072,6 +1073,91 @@ body.catalog-mode #find-differential-candidates {
   color: var(--muted);
 }
 
+.review-audit-card {
+  margin-top: 14px;
+  padding: 16px;
+}
+
+.review-audit-card h2,
+.review-audit-card h3 {
+  margin: 2px 0 0;
+}
+
+#periodic-fate-audit-status[data-status="complete"] {
+  color: var(--green);
+  border-color: var(--green);
+}
+
+#periodic-fate-audit-status[data-status="underdetermined"] {
+  color: #92400e;
+  border-color: var(--gold);
+  background: #fffbeb;
+}
+
+#periodic-fate-audit-status[data-status="invalid"] {
+  color: var(--rose);
+  border-color: var(--rose);
+  background: #fff7f7;
+}
+
+.audit-metrics {
+  display: grid;
+  grid-template-columns: repeat(6, minmax(100px, 1fr));
+  gap: 8px;
+  margin-top: 12px;
+}
+
+.audit-metrics div {
+  display: grid;
+  gap: 2px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  padding: 9px 11px;
+  background: #f8fafc;
+}
+
+.audit-metrics strong {
+  font-size: 18px;
+}
+
+.audit-metrics span,
+.audit-policy-note,
+.audit-obligation span {
+  color: var(--muted);
+  font-size: 11px;
+}
+
+.audit-policy-note {
+  margin: 10px 0;
+}
+
+.audit-obligations {
+  display: grid;
+  gap: 8px;
+}
+
+.audit-obligations h3 {
+  font-size: 13px;
+}
+
+.audit-obligation {
+  border-left: 3px solid var(--gold);
+  padding: 8px 10px;
+  background: #fffbeb;
+}
+
+.audit-obligation strong,
+.audit-obligation span {
+  display: block;
+}
+
+.audit-obligation ul {
+  margin: 5px 0 0;
+  padding-left: 18px;
+  color: #4b5563;
+  font-size: 12px;
+}
+
 .review-controls {
   display: grid;
   grid-template-columns: minmax(220px, 1fr) 190px 190px auto;
@@ -1721,6 +1807,10 @@ dialog.e2-presentation-dialog {
     grid-template-columns: 1fr;
   }
 
+  .audit-metrics {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+  }
+
   .review-graph-card #logic-graph-shell,
   .review-ledger-card {
     max-height: none;
diff --git a/backend/templates/index.html b/backend/templates/index.html
index 2be1528..a2ba657 100644
--- a/backend/templates/index.html
+++ b/backend/templates/index.html
@@ -370,6 +370,19 @@
       <div id="review-admission-summary" class="review-summary" aria-live="polite">Loading fact admission summary.</div>
     </section>
 
+    <section id="periodic-fate-audit" class="review-audit-card" aria-labelledby="periodic-fate-audit-title">
+      <div class="section-head">
+        <div>
+          <div class="eyebrow">HIGH FILTRATION FATE AUDIT</div>
+          <h2 id="periodic-fate-audit-title">Periodic vanishing coverage</h2>
+        </div>
+        <span id="periodic-fate-audit-status" class="pill">loading</span>
+      </div>
+      <div id="periodic-fate-audit-metrics" class="audit-metrics" aria-live="polite"></div>
+      <p id="formal-series-audit-note" class="audit-policy-note"></p>
+      <div id="periodic-fate-obligations" class="audit-obligations"></div>
+    </section>
+
     <section class="review-controls" aria-label="Review filters">
       <label>Workspace
         <select id="review-workspace-select" aria-label="Review workspace"></select>
diff --git a/public/static/app.js b/public/static/app.js
index 4f0a5f8..bb63790 100644
--- a/public/static/app.js
+++ b/public/static/app.js
@@ -4,6 +4,7 @@ const state = {
   catalogEntries: [],
   catalogMode: false,
   logicGraph: { nodes: [], edges: [] },
+  periodicFateLedger: null,
   history: { undo_depth: 0, redo_depth: 0, undo_label: null, redo_label: null },
   workspaceId: null,
   pageByWorkspace: new Map(),
@@ -1192,10 +1193,53 @@ function renderLogicGraph() {
     : `${nodes.length} typed nodes 路 ${edges.length} visible evidence edges.`;
 }
 
+function renderPeriodicFateAudit() {
+  const payload = state.periodicFateLedger || {};
+  const audit = payload.audit || {};
+  const ledger = payload.ledger || {};
+  const status = audit.status || "unavailable";
+  const statusNode = $("#periodic-fate-audit-status");
+  statusNode.textContent = status;
+  statusNode.dataset.status = status;
+
+  const metrics = [
+    ["Fact families", audit.fact_family_count],
+    ["Period mechanisms", audit.period_mechanism_count],
+    ["Module kinds", audit.module_kind_count],
+    ["Finite obligations", audit.finite_rank_obligation_count],
+    ["Covered", audit.covered_obligation_count],
+    ["Formal-series families", audit.formal_series_family_count],
+  ];
+  $("#periodic-fate-audit-metrics").innerHTML = metrics.map(([label, value]) => (
+    `<div><strong>${Number(value || 0)}</strong><span>${escapeHtml(label)}</span></div>`
+  )).join("");
+
+  const formalPolicy = ledger.formal_series_policy || {};
+  $("#formal-series-audit-note").textContent = formalPolicy.storage_rule
+    ? `Formal series are separate from finite-rank audit. ${formalPolicy.storage_rule}`
+    : "Formal-series families are handled separately from finite-rank obligations.";
+
+  const unresolved = (audit.obligations || []).filter((item) => !item.covered);
+  $("#periodic-fate-obligations").innerHTML = unresolved.length
+    ? `<h3>${unresolved.length} unresolved obligations</h3>${unresolved.map((item) => {
+      const reasons = (item.unresolved_reasons || []).length
+        ? item.unresolved_reasons.map((reason) => `<li>${escapeHtml(reason)}</li>`).join("")
+        : "<li>No resolution has been recorded.</li>";
+      const representative = item.period_class_id || item.id;
+      return `<article class="audit-obligation"><strong>${escapeHtml(item.id)}</strong><span>${escapeHtml(representative)}</span><ul>${reasons}</ul></article>`;
+    }).join("")}`
+    : '<p class="empty">All recorded high-filtration obligations are covered.</p>';
+}
+
 async function loadReviewPage() {
-  const [project, logicGraph] = await Promise.all([api("/api/project"), api("/api/v2/logic-graph")]);
+  const [project, logicGraph, periodicFateLedger] = await Promise.all([
+    api("/api/project"),
+    api("/api/v2/logic-graph"),
+    api("/api/v2/review/periodic-fate-ledger"),
+  ]);
   state.project = project;
   state.logicGraph = logicGraph;
+  state.periodicFateLedger = periodicFateLedger;
   if (!project.workspaces.some((item) => item.id === state.workspaceId)) state.workspaceId = defaultWorkspaceId();
   const selector = $("#review-workspace-select");
   selector.innerHTML = project.workspaces.map((item) => (
@@ -1208,6 +1252,7 @@ async function loadReviewPage() {
     + `<span>${Number(admission.review_queue || 0)} in review</span>`
     + `<small>${escapeHtml(admission.policy || "Premise-complete facts enter the admitted DAG.")}</small>`
   );
+  renderPeriodicFateAudit();
   renderProofTree();
   syncLayoutHeight();
 }
diff --git a/public/static/style.css b/public/static/style.css
index 4ae8314..abcb1b5 100644
--- a/public/static/style.css
+++ b/public/static/style.css
@@ -1027,6 +1027,7 @@ body.catalog-mode #find-differential-candidates {
 }
 
 .review-intro,
+.review-audit-card,
 .review-controls,
 .review-graph-card,
 .review-ledger-card {
@@ -1072,6 +1073,91 @@ body.catalog-mode #find-differential-candidates {
   color: var(--muted);
 }
 
+.review-audit-card {
+  margin-top: 14px;
+  padding: 16px;
+}
+
+.review-audit-card h2,
+.review-audit-card h3 {
+  margin: 2px 0 0;
+}
+
+#periodic-fate-audit-status[data-status="complete"] {
+  color: var(--green);
+  border-color: var(--green);
+}
+
+#periodic-fate-audit-status[data-status="underdetermined"] {
+  color: #92400e;
+  border-color: var(--gold);
+  background: #fffbeb;
+}
+
+#periodic-fate-audit-status[data-status="invalid"] {
+  color: var(--rose);
+  border-color: var(--rose);
+  background: #fff7f7;
+}
+
+.audit-metrics {
+  display: grid;
+  grid-template-columns: repeat(6, minmax(100px, 1fr));
+  gap: 8px;
+  margin-top: 12px;
+}
+
+.audit-metrics div {
+  display: grid;
+  gap: 2px;
+  border: 1px solid var(--line);
+  border-radius: 8px;
+  padding: 9px 11px;
+  background: #f8fafc;
+}
+
+.audit-metrics strong {
+  font-size: 18px;
+}
+
+.audit-metrics span,
+.audit-policy-note,
+.audit-obligation span {
+  color: var(--muted);
+  font-size: 11px;
+}
+
+.audit-policy-note {
+  margin: 10px 0;
+}
+
+.audit-obligations {
+  display: grid;
+  gap: 8px;
+}
+
+.audit-obligations h3 {
+  font-size: 13px;
+}
+
+.audit-obligation {
+  border-left: 3px solid var(--gold);
+  padding: 8px 10px;
+  background: #fffbeb;
+}
+
+.audit-obligation strong,
+.audit-obligation span {
+  display: block;
+}
+
+.audit-obligation ul {
+  margin: 5px 0 0;
+  padding-left: 18px;
+  color: #4b5563;
+  font-size: 12px;
+}
+
 .review-controls {
   display: grid;
   grid-template-columns: minmax(220px, 1fr) 190px 190px auto;
@@ -1721,6 +1807,10 @@ dialog.e2-presentation-dialog {
     grid-template-columns: 1fr;
   }
 
+  .audit-metrics {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+  }
+
   .review-graph-card #logic-graph-shell,
   .review-ledger-card {
     max-height: none;
diff --git a/tests/test_page_modes.py b/tests/test_page_modes.py
index cfce5d0..dffb781 100644
--- a/tests/test_page_modes.py
+++ b/tests/test_page_modes.py
@@ -32,6 +32,8 @@ class PageModeTests(unittest.TestCase):
         self.assertIn('id="logic-graph"', markup)
         self.assertIn('id="proof-tree"', markup)
         self.assertIn('id="logic-node-detail"', markup)
+        self.assertIn('id="periodic-fate-audit"', markup)
+        self.assertIn('id="periodic-fate-obligations"', markup)
         self.assertNotIn('id="chart"', markup)
         self.assertNotIn('aria-label="Chart controls"', markup)
 
```
